name: ci

on:
  push:
    branches: [ master, develop ]
  pull_request:
    types: [ synchronize, opened ]
  workflow_dispatch:

jobs:
  bsb:
    runs-on: ubuntu-latest
    steps:
      - name: Show environment
        run: |
          ruby -v
          python -v

      - uses: actions/checkout@v2
        with:
          ref: ${{ github.head_ref }}

      - name: Run buildstockbatch
        run: |
          pip install git+https://github.com/NREL/buildstockbatch.git@develop
          
          buildstock_docker project_national/national_baseline.yml
          buildstock_docker project_testing/testing_baseline.yml
          
          buildstock_docker project_national/national_upgrades.yml
          buildstock_docker project_testing/testing_upgrades.yml

      - name: Upload project folders
        uses: actions/upload-artifact@v2
        with:
          path: |
            project_testing/testing_baseline
            project_national/national_baseline
            project_testing/testing_upgrades
            project_national/national_upgrades
          name: projects

      - name: Process results
        run: |
          gunzip -v project_national/national_baseline/results_csvs/*.csv.gz
          gunzip -v project_testing/testing_baseline/results_csvs/*.csv.gz
          
          gunzip -v project_national/national_upgrades/results_csvs/*.csv.gz
          gunzip -v project_testing/testing_upgrades/results_csvs/*.csv.gz
          
          pip install pandas
          python test/util.py

      - name: Upload integration results
        uses: actions/upload-artifact@v2
        with:
          path: |
            baseline
            upgrades
          name: results

  build:
    runs-on: ubuntu-latest
    needs: bsb
    container:
      image: docker://nrel/openstudio:3.3.0
    steps:
      - name: Show environment
        run: |
          bundle -v
          ruby -v

      - uses: actions/checkout@v2
        with:
          ref: ${{ github.head_ref }}

      - name: Install gems
        run: |
          rm -f Gemfile.lock && bundle install

      - name: Update all measures
        run: |
          ruby tasks.rb update_measures

      - name: Regenerate test osms
        run: |
          rake test:regenerate_osms

      - name: Run all unit tests
        run: |
          rake test:unit_tests

      - name: Run all project tests
        run: |
          rake test:project_tests

      - name: Run local analysis tests
        run: |
          rake test:analysis_tests

      - name: Download project folders
        uses: actions/download-artifact@v2
        with:
          path: projects
          name: projects

      - name: Run bsb tests
        run: |
          ruby test/test_bsb_analysis.rb

      - name: Upload feature samples
        uses: actions/upload-artifact@v2
        with:
          path: resources/buildstock.csv
          name: samples

      - name: Upload schedules
        uses: actions/upload-artifact@v2
        with:
          path: resources/measures/ResidentialScheduleGenerator/tests/output/test_sweep_building_ids_and_num_occupants
          name: schedules

  compare-results:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Show environment
        run: |
          ruby -v
          python -v

      - uses: actions/checkout@v2
        with:
          ref: ${{ github.event.pull_request.base.sha }}

      - name: Upload base samples and results
        uses: actions/upload-artifact@v2
        with:
          path: |
            test/test_samples_osw/baseline
            test/test_samples_osw/upgrades
          name: base

      - uses: actions/checkout@v2
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Download base samples and results
        uses: actions/download-artifact@v2
        with:
          path: base
          name: base

      - name: Download feature samples
        uses: actions/download-artifact@v2
        with:
          path: samples
          name: samples

      - name: Download feature results
        uses: actions/download-artifact@v2
        with:
          path: results
          name: results

      - name: Compare samples and results
        run: |
          pip install numpy
          pip install pandas
          pip install plotly
          pip install kaleido
          
          mkdir test/test_samples_osw/comparisons
          mkdir test/test_samples_osw/comparisons/baseline
          mkdir test/test_samples_osw/comparisons/upgrades
          
          python test/compare.py -a samples -b base/baseline -f samples -e test/test_samples_osw/comparisons/baseline
          
          python test/compare.py -a results -b base/baseline -f results/baseline -e test/test_samples_osw/comparisons/baseline
          python test/compare.py -a results -af sum -ac geometry_building_type_recs -x results_output_building_type_sum.csv -b base/baseline -f results/baseline -e test/test_samples_osw/comparisons/baseline
          
          python test/compare.py -a visualize -dc geometry_building_type_recs -x results_output_building_type.html -b base/baseline -f results/baseline -e test/test_samples_osw/comparisons/baseline
          python test/compare.py -a visualize -dc geometry_foundation_type -x results_output_foundation_type.html -b base/baseline -f results/baseline -e test/test_samples_osw/comparisons/baseline
          python test/compare.py -a visualize -dc county -x results_output_county.html -b base/baseline -f results/baseline -e test/test_samples_osw/comparisons/baseline
          python test/compare.py -a visualize -dc geometry_building_type_recs -ac county -af sum -x results_output_county_sum.html -b base/baseline -f results/baseline -e test/test_samples_osw/comparisons/baseline
                    
          python test/compare.py -a results -b base/upgrades -f results/upgrades -e test/test_samples_osw/comparisons/upgrades
          
          python test/compare.py -a visualize -x results_output.html -b base/upgrades -f results/upgrades -e test/test_samples_osw/comparisons/upgrades

      - name: Upload comparisons
        uses: actions/upload-artifact@v2
        with:
          path: test/test_samples_osw/comparisons
          name: comparisons

  update-results:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.head_ref }}

      - name: Download feature samples
        uses: actions/download-artifact@v2
        with:
          path: samples
          name: samples

      - name: Download feature results
        uses: actions/download-artifact@v2
        with:
          path: results
          name: results

      - name: Commit latest results
        shell: bash        
        run: |
          branch_name="${{ github.head_ref }}"
          git pull origin $branch_name
          cp -r samples/buildstock*.csv test/test_samples_osw/baseline
          cp -r results/baseline/results*.csv test/test_samples_osw/baseline
          cp -r results/upgrades/results*.csv test/test_samples_osw/upgrades
          git add test/test_samples_osw/baseline
          git add test/test_samples_osw/upgrades
          git status
          if [[ $(git diff --cached --exit-code) ]]; then
            git config --global user.email "github-action@users.noreply.github.com"
            git config --global user.name "GitHub Action"
            git commit -m "Latest results."
            echo "Pushing to branch: $branch_name"
            git push -u origin $branch_name
          fi
