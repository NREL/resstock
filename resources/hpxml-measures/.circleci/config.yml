version: 2
jobs:
  build:
    docker:
      - image: nrel/openstudio:3.0.0-beta
    steps:
      - checkout
      - run: 
          name: Install gems
          command: |
            rm -f Gemfile.lock && bundle install
      - run: 
          name: Run tests
          command: |
            rake test_all
      - store_artifacts:
          path: workflow/tests/results
          destination: results
